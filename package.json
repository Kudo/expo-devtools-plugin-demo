{
  "private": true,
  "name": "monorepo",
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ]
  },
  "scripts": {
    "postinstall": "patch-package"
  },
  "dependencies": {
    "patch-package": "^8.0.0",
    "postinstall-postinstall": "^2.1.0"
  },
  "devDependencies": {
    "prettier": "^3.0.3"
  }
}
